apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lop-postgres-data
  namespace: life-observability-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
